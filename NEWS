nat.as 0.6.1
==========
* fix bug in downloading AnalysisSuite from github (#1)
* dev: minor build tidying

nat.as 0.6
==========
* Add nat (>0.2) as dependency

nat.as 0.5
==========
* Add nat.utils, rgl, RANN and igraph packages as dependencies

nat.as 0.4.1
============
* safer developer mode install with install_analysis_suite()
* new is_analysis_suite_installed() function
* package startup message confined to .onAttach
* documentation improvements

nat.as 0.4
==========
* significantly more mature package
* loads all code with library(nat.as)
* reload_analysis_suite() / test_analysis_suite() can be used when hacking the
  AnalysisSuite codebase.

nat.as 0.3
==========
* first functional version
* still need to decide on best approach for loading code
  should this happen automatically when library(nat.as) is called?